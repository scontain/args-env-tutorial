name: $NAMESPACE
version: "0.3.10"
predecessor: $NAMESPACE_PREDECESSOR

# Only creator can access this policy
# and only creator can use this namespace

access_policy:
  read:
   - CREATOR
  update:
   - CREATOR
  create_sessions:
    - CREATOR
